# План внедрения раздела статистики по счетам

## Краткое резюме
Нужно переработать навигацию "Статистика" в боковом меню, оставить текущую статистику по стратегиям без смены URL, добавить раздел по счетам со списком на `/statistics/accaunts` и страницей детальной статистики счета, где строится график и таблица по данным `accaunt_inflation` с тремя временными рядами.

## Факты (перепроверено по проекту)
- Верхние ссылки "🪙 Золотой запас" и "💰 Вклад" находятся в `templates/base.admin.html.twig`.
- Пункт "Статистика" в левом меню сейчас одиночный и ведет на `app_statistics_strategies_list` в `templates/widgets/left-sidebar/show.html.twig`.
- Текущая статистика по стратегиям реализована в `src/Controller/StatisticsController.php` и шаблонах `templates/statistics/list.by.strategies.html.twig`, `templates/statistics/strategy.trades.html.twig` (URL `/statistics/strategies` и `/statistics/strategy/{strategyId}/accaunt/{accauntId}`).
- График истории счета реализован на `/accaunt/{id}/history` через `src/Controller/AccauntHistoryController.php`, `templates/accaunt_history/list.html.twig`, `public/vendor/js/accaunt_history.js`.
- Данные по инфляции хранятся в `accaunt_inflation` и описаны сущностью `src/Entity/AccauntInflation.php` (поля `accauntDepositBalance`, `accauntBalance`, `accauntInflationBalance`).

## Пошаговый план реализации
1. **Навигация и UI**
   - Удалить верхние ссылки "Золотой запас" и "Вклад" из `templates/base.admin.html.twig`.
   - Переделать пункт "Статистика" в `templates/widgets/left-sidebar/show.html.twig` в выпадающий (treeview) с подпунктами:
     - "По стратегиям" → `app_statistics_strategies_list` (URL без изменений).
     - "По счетам" → новый маршрут списка счетов статистики.
   - Уточнить и проставить классы active/open для корректной подсветки пункта меню (по текущему URL).

2. **Новый список "По счетам"**
   - Добавить маршрут `/statistics/accaunts` (название маршрута `app_statistics_accaunts_list`) в `src/Controller/StatisticsController.php`.
   - Получать список уникальных счетов через `AccauntRepository`.
   - Создать шаблон `templates/statistics/list.by.accaunts.html.twig`, по структуре ближе к `templates/accaunt/index.html.twig`:
     - колонки: `#`, `Название`, `Код`, `Дата создания`, кнопка перехода.
   - Данные таблицы формировать как список счетов (без стратегий).

3. **Детальная статистика по счету**
   - Создать маршрут `/statistics/accaunt/{id}` (именно `accaunt`, как в текущем нейминге проекта).
   - Реализовать сервис/метод для получения данных по `AccauntInflation`:
     - выборка по `accaunt_id`, сортировка по `date` ASC;
     - формирование массива меток (дата) и 3 рядов значений:
       - `accauntDepositBalance`
       - `accauntBalance`
       - `accauntInflationBalance`
   - Добавить шаблон, например `templates/statistics/accaunt.inflation.html.twig`:
     - график по образцу страницы `/accaunt/{id}/history`;
     - таблицу значений `accaunt_inflation` под графиком;
     - заголовок, хлебные крошки и базовую структуру страницы (как в статистике по стратегиям).

4. **График с 3 рядами**
   - Добавить новый JS-скрипт (например, `public/vendor/js/accaunt_inflation.js`):
     - читать JSON с тремя наборами данных;
     - отрисовать 3 линии с легендой;
     - настроить цвета и подписи (например, "Вклад", "Счет", "Инфляция").
   - Подключить Chart.js и новый скрипт в шаблоне детальной статистики по счету.

5. **Проверка и качество**
   - Убедиться, что URL и параметры существующей статистики по стратегиям не изменились.
   - Проверить корректность сортировки дат и выравнивание рядов (одинаковые метки по датам).
   - Обновить/добавить тесты при необходимости (минимум — smoke через открытие новых страниц).
   - После реализации запустить полный прогон `./test.sh` и устранить возможные регрессии.

## Архитектура функционала (классы, методы, логика)

### Изменяемые классы

**`App\Controller\StatisticsController`**
- Конструктор:
  - Инжектировать `TradeRepository`, `AccauntRepository`, `AccauntInflationRepository` и форматтер данных графика через DI.
  - Использовать их как приватные свойства, без прокидывания через параметры методов.
- `listByAccaunts(): Response`
  - Вызвать `AccauntRepository` (через свойство) для получения списка счетов.
  - Сформировать список только из счетов (без стратегии).
  - Передать данные в `templates/statistics/list.by.accaunts.html.twig`.
- `accauntInflation(int $id): Response`
  - Получить `Accaunt` через `AccauntRepository` (через свойство).
  - Если счет не найден — `404`.
  - Получить список `AccauntInflation` через `AccauntInflationRepository::findByAccauntIdOrdered`.
  - Преобразовать данные в формат графика через отдельный класс-форматтер (см. ниже).
  - Отрендерить `templates/statistics/accaunt.inflation.html.twig` с данными графика и таблицы.

**`App\Repository\TradeRepository`**
- Без изменений для раздела "По счетам".
- Если ранее добавлялся метод `getAccauntsByStrategies`, его нужно удалить или перестать использовать (список формируется через `AccauntRepository`).

**`App\Repository\AccauntInflationRepository`**
- Новый метод:
  - `findByAccauntIdOrdered(int $accauntId): array`
  - Логика: `findBy(['accaunt' => $accauntId], ['date' => 'ASC'])`.

### Новые классы

**`App\Service\AccauntInflation\AccauntInflationGraphDataFormatter`** (новый)
- Назначение: преобразовать массив `AccauntInflation` в JSON-формат для графика (без доступа к БД).
- Методы:
  - `format(array $items): string`
    - Сформировать `labels` из `date` (`Y-m-d`).
    - Собрать 3 массива значений:
      - `accaunt_deposit_balance` ← `getAccauntDepositBalance()`.
      - `accaunt_balance` ← `getAccauntBalance()`.
      - `accaunt_inflation_balance` ← `getAccauntInflationBalance()`.
    - Вернуть `json_encode(['labels' => ..., 'datasets' => ...])` с понятными `label` для каждой линии.
    - Формат данных должен быть совместим с текущими графиками (ориентир — `AccauntHistoryService::formatGraphData`).
  - `formatGraphData(array $items): string` (private)
    - Собрать `labels` из `date` (`Y-m-d`).
    - Собрать 3 массива значений:
      - `accaunt_deposit_balance` ← `getAccauntDepositBalance()`.
      - `accaunt_balance` ← `getAccauntBalance()`.
      - `accaunt_inflation_balance` ← `getAccauntInflationBalance()`.
    - Вернуть `json_encode(['labels' => ..., 'datasets' => ...])` с понятными `label` для каждой линии.

### Новые шаблоны

**`templates/statistics/list.by.accaunts.html.twig`**
- Структура и таблица такие же, как в `templates/accaunt/index.html.twig`.
- Колонки: `#`, `Название`, `Код`, `Дата создания`, кнопка.
- Кнопка ведет на `/statistics/accaunt/{id}`.

**`templates/statistics/accaunt.inflation.html.twig`**
- Заголовок: "Статистика по счету: {название}".
- Блок с графиком (по примеру `templates/accaunt_history/list.html.twig`).
- Таблица значений `accaunt_inflation` под графиком:
  - Порядок колонок: `Дата`, `Пополнение`, `Ставка ЦБ`, `изменение при инфляции`, `Процент по вкладу`, `Вклад`, `Счет`.
  - Маппинг полей:
    - `Пополнение` → `movementAmount` (знак `+` не добавлять).
    - `Ставка ЦБ` → `centralBankKeyRate` с `%`, 2 знака.
    - `изменение при инфляции` → `accauntInflationBalance`.
    - `Процент по вкладу` → `depositRate` с `%`, 2 знака.
    - `Вклад` → `accauntDepositBalance`.
    - `Счет` → `accauntBalance`.
  - Форматирование чисел и дат через Twig-фильтры.

### Новые JS-скрипты

**`public/vendor/js/accaunt_inflation.js`**
- Читает `data-graph-data-encode` с полями `labels` и `datasets`.
- Рисует 3 линии с легендой.
- Цвета и подписи:
  - "Вклад" (`accaunt_deposit_balance`)
  - "Счет" (`accaunt_balance`)
  - "Инфляция" (`accaunt_inflation_balance`)

### Маршруты (новые)

- `/statistics/accaunts` → `StatisticsController::listByAccaunts` (название `app_statistics_accaunts_list`).
- `/statistics/accaunt/{id}` → `StatisticsController::accauntInflation` (название `app_statistics_accaunt`).

## Вопросы/допущения
1. Колонки списка "По счетам": как в списке счетов (`#`, `Название`, `Код`, `Дата создания`, кнопка) — подтверждено.
2. Таблица значений `accaunt_inflation` под графиком — подтверждено.
3. URL списка "По счетам": `/statistics/accaunts` — подтверждено.
4. Фильтры/диапазон дат на графике не нужны — подтверждено.
5. `Пополнение` → `movementAmount`, знак `+` не показывать — подтверждено.
6. `изменение при инфляции` → `accauntInflationBalance` — подтверждено.
7. Ставки выводить с `%` и 2 знаками — подтверждено.
